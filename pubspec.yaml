name: appredmine_certto
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  http: ^0.13.6
  crypto: ^3.0.3
  get_storage: ^2.1.1
  image_picker: ^1.0.0
  url_launcher: ^6.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_fiber.png
    - assets/images/login_background.png
    - assets/images/icons/dispatcher.png
    - assets/images/icons/inventory.png